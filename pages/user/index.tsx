// Check if user is signed in, if they are redirect to /user/id, otherwise redirect to home landing page.

const Index = () => {
    return (
        <div>

        </div>
    )
}

export default Index;